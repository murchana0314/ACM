//5. Password Length: A password must be at least 8 characters long. The entered password has 12 characters. Check if it meets the requirement. 
#include<stdio.h>

int main()
{
    int password_characters = 12;

    printf("Reset the password", password_characters <= 8);

    return 0;
}